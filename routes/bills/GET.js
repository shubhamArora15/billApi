let bills = require(process.env.PWD + "/models/bills");
let customer = require(process.env.PWD + "/models/customer");

var get_bills = function(req, res, next) {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    get_bills : get_bills
}